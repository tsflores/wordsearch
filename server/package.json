{
  "name": "server",
  "version": "1.0.0",
  "description": "simple server to run word search app",
  "license": "ISC",
  "author": "Trinidad Flores",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "start-dev": "nodemon --inspect app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
